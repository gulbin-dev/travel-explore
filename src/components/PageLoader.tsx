export default function PageLoader() {
  return (
    <div className="text-font-dark bg-primary fixed inset-0 z-50 flex h-screen w-screen items-center justify-center">
      <p className="text-size-xxl">Loading...</p>
    </div>
  );
}
